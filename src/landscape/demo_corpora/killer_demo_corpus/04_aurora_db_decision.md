# ADR-014: Aurora Datastore Selection

After evaluating PostgreSQL, MongoDB, and DynamoDB, the team selected PostgreSQL for Project Aurora's primary datastore.

Maya Chen approved PostgreSQL as the canonical choice for Aurora's storage layer.

The decision was driven by the need for strong relational consistency and rich querying over robot telemetry.
