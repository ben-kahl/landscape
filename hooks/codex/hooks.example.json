{
  "_comment": "OUTDATED / UNSUPPORTED — these codex hooks targeted the removed push-based HTTP capture endpoints. Automatic capture is currently Claude Code only (end-of-session transcript-pull; see hooks/claude-code/settings.example.json and `landscape ingest-transcript`). Do not use until a codex transcript reader lands.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"$LANDSCAPE_HOME/scripts/landscape_capture_hook.py\" codex",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"$LANDSCAPE_HOME/scripts/landscape_capture_hook.py\" codex",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
